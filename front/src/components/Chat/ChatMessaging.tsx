// import { useChatContext } from '../../hooks/useChatContext';
import '../../style/components/chat/chat-container.css';

const ChatMessaging = () => {
  // const { setShowChannel } = useChatContext();
  return (
		<>
		</>
	);
};

export default ChatMessaging;
