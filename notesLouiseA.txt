- remove Game and FR notif in front when we remove the notification (when we click on the NavBar Page Link)
- faire en sorte de refresh a chaque fois qu'on click sur un NavLink
- still receiving 2 notif when sending a message
- need to receive notif when we create a channel group (or maybe we don't care)